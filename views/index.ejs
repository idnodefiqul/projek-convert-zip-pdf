<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ZIP to PDF Converter</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-gradient-to-br from-purple-600 via-blue-500 to-teal-400 min-h-screen flex items-center justify-center">
  <div class="bg-white bg-opacity-90 p-8 rounded-2xl shadow-2xl max-w-lg w-full transform hover:scale-105 transition-transform duration-300">
    <h1 class="text-4xl font-extrabold text-center mb-6 text-transparent bg-clip-text bg-gradient-to-r from-pink-500 to-yellow-500">ZIP to PDF Converter</h1>
    <p class="text-center text-gray-600 mb-6">Convert your images to stunning PDFs with ease!</p>
    <form id="uploadForm" enctype="multipart/form-data" class="space-y-6">
      <div>
        <label class="block text-gray-700 font-semibold mb-2" for="zipFile">Upload ZIP File (Max 500MB)</label>
        <input type="file" name="zipFile" id="zipFile" accept=".zip" class="w-full p-3 border-2 border-purple-300 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-colors" required>
      </div>
      <div>
        <label class="block text-gray-700 font-semibold mb-2" for="quality">Select Quality</label>
        <select name="quality" id="quality" class="w-full p-3 border-2 border-purple-300 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-colors">
          <option value="medium">Medium (100 DPI)</option>
          <option value="hd" selected>HD (150 DPI)</option>
          <option value="fullhd">Full HD (300 DPI)</option>
        </select>
      </div>
      <button type="submit" class="w-full bg-gradient-to-r from-pink-500 to-purple-600 text-white p-3 rounded-lg hover:from-pink-600 hover:to-purple-700 font-semibold transition-all duration-300">Convert to PDF</button>
    </form>
    <div id="loading" class="hidden mt-6 text-center">
      <svg class="animate-spin h-8 w-8 mx-auto text-blue-500" viewBox="0 0 24 24">
        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v8h8a8 8 0 01-8 8 8 8 0 01-8-8z"></path>
      </svg>
      <p class="text-gray-700 mt-2">Converting... You can close the tab, we'll notify you!</p>
    </div>
    <div id="notification" class="hidden mt-6 p-4 bg-gradient-to-r from-green-400 to-teal-500 rounded-lg text-white text-center">
      <div class="flex justify-center mb-2">
        <svg class="h-12 w-12 animate-bounce" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z" fill="white"/>
        </svg>
      </div>
      <p class="font-semibold">Awesome! Your PDF is ready!</p>
      <a id="downloadLink" href="#" download class="inline-block mt-2 bg-white text-teal-600 px-4 py-2 rounded-lg hover:bg-gray-100 transition-colors">Download Now</a>
    </div>
  </div>
  <script src="/js/main.js"></script>
</body>
</html>
